import { DndropDirective } from './dndrop.directive';

describe('DndropDirective', () => {
  it('should create an instance', () => {
    const directive = new DndropDirective();
    expect(directive).toBeTruthy();
  });
});
